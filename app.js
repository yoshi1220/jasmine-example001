function subtract(minued, subtrahend) {
  return minued - subtrahend;
}

function add(addend, number) {
  return addend + number;
}

function sortAscending(positiveIntegersString) {
  numbers = positiveIntegersString.split(/\n/);
  console.log(numbers);
  console.log(numbers);
  return numbers.sort();
}
